  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <p class="btn">
        <%= link_to "Shop All", root_path, class: "navbar-brand", :style=> "color:#820263;" %>
        </p>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <ul class="nav navbar-nav">
         <li class="dropdown btn">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color:#820263;"> Shop by Category <span class="caret"></span></a>
           <ul class="dropdown-menu">
            <% @categories.each do |cat| %>
            <li><%= link_to cat.name, categorical_path(category_id: cat.id), :style=> "color:#820263;"%></li>
               <% end %>
           </ul>
        </li>  
        <li class="dropdown btn">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color:#820263;"> Shop by Brand <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% @brands.each do |brand| %>
              <li><%= link_to brand, branding_path(brand:brand), :style=> "color:#820263;"%></li>
              <% end %>
          </ul>
         </li>
     </ul>

        <ul class="nav navbar-nav navbar-right">
            <li class="btn">
              <%= link_to " View Cart",view_order_path, :style=> "color:#820263;"  %>
            </li>
            <% if current_user %>
              <li class="btn"><%= link_to "Signout", destroy_user_session_path, method: :delete, :style=> "color:#820263;"  %></li>
            <% else %>
              <li class="btn"><%= link_to "Log in", new_user_session_path, :style=> "color:#820263;"%></li>
            <%end%>
          
        </ul>
      </div>
    </div>
  </nav>