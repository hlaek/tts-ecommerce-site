<div class="row">
	<% @products.each do |product| %>
		<div class="col-md-4">
			<div class="well">
				<% if product.image.url.nil? == false %>
				<p> <%= image_tag product.image.url, size: 200 %></p>
				<% end %>
				<h3><%= link_to product.name, product, :style=>  'color:#2E294E;'%></h3>
				<h5><%= number_to_currency product.price, :style=> 'color:#2E294E;'%></p>
				<h5>
					<a href="#" data-toggle="modal" data-target="#myModal_<%= product.id %>">
						<button class="glyphicon glyphicon-plus btn">Quick Info</button>
					</a>
				</p>
			</div>
		</div>

					<div class="modal fade" id="myModal_<%= product.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
			  <div class="modal-dialog" role="document">
			    <div class="modal-content center-it">
			      <div class="modal-header">
			        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
			      </div>
			      <div class="modal-body">
			        <% if product.image.url.nil? == false %>
			          <p><%= image_tag product.image.url, size: 400, class: "storefront-pics" %></p>
			        <% end %>
			        <h3><%= link_to product.name, product %></h3>
			        <p><%= number_to_currency product.price %></p>
			        <p><%= product.description %></p>
			        <p>Brand: <%= product.brand %></p>
			      </div>
			      <div class="modal-footer">
			        <p>
			        	<%= form_tag add_to_cart_path do %>
			        	<%= hidden_field_tag :product_id, product.id %>
			        	<%= number_field_tag :quantity, 1, placeholder: "How many?" %>
			        	<%= submit_tag "Add to Cart", class: "btn add_to_cart_btn" %>
			        	<% end %>
			        </p>
			        <p>
			          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
			        </p>
			      </div>
			    </div>
			  </div>
			</div>
	<% end %>
</div>